<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>3379. Transformed Array</title>
    <link rel="stylesheet" href="/styles.min.css" />
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>
<body>
    <main>
        <h1>LeetCode 3379: Transformed Array (C++)</h1>

        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given an integer array nums that represents a circular array. Your task is to create a new array result of the same size, following these rules: For each index i (where 0 &lt;= i &lt; nums.length), perform the following independent actions: If nums[i] &gt; 0: Start at index i and move nums[i] steps to the right in the circular array. Set result[i] to the value of the index where you land. If nums[i] &lt; 0: Start at index i and move abs(nums[i]) steps to the left in the circular array. Set result[i] to the value of the index where you land. If nums[i] == 0: Set result[i] to nums[i]. Return the new array result.
            </p>
            <p><strong>Example:</strong>
                Input: nums = [3,-2,1,1]<br/>
                Output: [1,1,1,3]<br/>
                Explanation: For nums[0] which is 3, move 3 steps to the right to reach nums[3], so result[0] = 1. For nums[1] which is -2, move 2 steps to the left to reach nums[3], so result[1] = 1. For nums[2] which is 1, move 1 step to the right to reach nums[3], so result[2] = 1. For nums[3] which is 1, move 1 step to the right to reach nums[0], so result[3] = 3.<br/><br/>
                Input: nums = [-1,4,-1]<br/>
                Output: [-1,-1,4]<br/>
                Explanation: For nums[0] which is -1, move 1 step to the left to reach nums[2], so result[0] = -1. For nums[1] which is 4, move 4 steps to the right to reach nums[2], so result[1] = -1. For nums[2] which is -1, move 1 step to the left to reach nums[1], so result[2] = 4.
            </p>
        </div>

        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    vector&lt;int&gt; constructTransformedArray(vector&lt;int&gt;&amp; nums) {
        int n = nums.size();
        vector&lt;int&gt; result(n);
        
        for (int i = 0; i &lt; n; i++) {
            if (nums[i] == 0) {
                result[i] = nums[i];
            } else if (nums[i] &gt; 0) {
                // Move right
                int newIndex = (i + nums[i]) % n;
                result[i] = nums[newIndex];
            } else {
                // Move left
                int steps = abs(nums[i]) % n;
                int newIndex = (i - steps + n) % n;
                result[i] = nums[newIndex];
            }
        }
        
        return result;
    }
};</code></pre>
        </div>

        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>
</html>