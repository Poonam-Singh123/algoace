<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 812: Largest Triangle Area (C++)
    </title>
    <link rel="stylesheet" href="/styles.min.css" />
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 812: Largest Triangle Area (C++)</h1>

        <div class="problem-description">
            <p><strong>Problem:</strong>
                Given an array of points on the X-Y plane points where points[i] = [xi, yi], return the area of the largest triangle that can be formed by any three different points. The area is calculated using the coordinate geometry formula. Answers within 10^-5 of the actual answer will be accepted.
            </p>
            <p><strong>Example:</strong>
                Input: points = [[0,0],[0,1],[1,0],[0,2],[2,0]]<br>
                Output: 2.00000<br>
                Explanation: The five points are shown in the above figure. The triangle with the largest area is formed by points [0,2], [2,0], [0,0] with area = 2.0.
            </p>
        </div>

        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    double largestTriangleArea(vector&lt;vector&lt;int&gt;&gt;&amp; points) {
        double maxArea = 0.0;
        int n = points.size();
        
        // Try all possible combinations of 3 points
        for (int i = 0; i &lt; n; i++) {
            for (int j = i + 1; j &lt; n; j++) {
                for (int k = j + 1; k &lt; n; k++) {
                    // Calculate area using coordinate geometry formula
                    double area = calculateTriangleArea(points[i], points[j], points[k]);
                    maxArea = max(maxArea, area);
                }
            }
        }
        
        return maxArea;
    }
    
private:
    double calculateTriangleArea(const vector&lt;int&gt;&amp; p1, const vector&lt;int&gt;&amp; p2, const vector&lt;int&gt;&amp; p3) {
        // Using the cross product formula for triangle area:
        // Area = 0.5 * |x1(y2 - y3) + x2(y3 - y1) + x3(y1 - y2)|
        // This is derived from the determinant formula for triangle area
        
        int x1 = p1[0], y1 = p1[1];
        int x2 = p2[0], y2 = p2[1];
        int x3 = p3[0], y3 = p3[1];
        
        // Calculate the area using the shoelace formula
        double area = abs(x1 * (y2 - y3) + x2 * (y3 - y1) + x3 * (y1 - y2)) / 2.0;
        
        return area;
    }
};</code></pre>
        </div>

        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>