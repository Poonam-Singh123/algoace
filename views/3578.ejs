<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>3578. Construct 2D Grid Matching Graph Layout</title>
    <link rel="stylesheet" href="/styles.min.css" />
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>
<body>
    <main>
        <h1>LeetCode 3578: Construct 2D Grid Matching Graph Layout (C++)</h1>

        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given a 2D integer array edges representing an undirected graph having n nodes, where edges[i] = [u<sub>i</sub>, v<sub>i</sub>] indicates that there exists an edge between nodes u<sub>i</sub> and v<sub>i</sub>. Construct a 2D grid that satisfies these conditions: The grid contains all nodes from 0 to n - 1. Two nodes should be in adjacent grid cells (horizontally or vertically) if and only if there exists an edge between them in edges. It is guaranteed that edges can form a 2D grid that satisfies the conditions. Return a 2D integer array satisfying the conditions above. If there are multiple solutions, return any of them.
            </p>
            <p><strong>Example:</strong>
                Input: n = 4, edges = [[0,1],[0,2],[1,3],[2,3]]<br/>
                Output: [[3,1],[2,0]]<br/>
                Explanation: We can arrange the grid as shown. It can be shown that there exists an edge between all adjacent cells.<br/><br/>
                Input: n = 5, edges = [[0,1],[1,3],[2,3],[2,4]]<br/>
                Output: [[4,2,3,1,0]]<br/>
                Explanation: We can arrange the nodes in a single row.<br/><br/>
                Input: n = 9, edges = [[0,1],[0,4],[0,5],[1,7],[2,3],[2,4],[2,5],[3,6],[4,6],[4,7],[6,8],[7,8]]<br/>
                Output: [[8,6,3],[7,4,2],[1,0,5]]<br/>
                Explanation: This forms a valid 3x3 grid matching the graph layout.
            </p>
        </div>

        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    vector&lt;vector&lt;int&gt;&gt; constructGridLayout(int n, vector&lt;vector&lt;int&gt;&gt;&amp; edges) {
        // Build adjacency list
        vector&lt;vector&lt;int&gt;&gt; adj(n);
        for (auto&amp; edge : edges) {
            adj[edge[0]].push_back(edge[1]);
            adj[edge[1]].push_back(edge[0]);
        }
        
        // Find corner node (node with degree 2 in a grid)
        int corner = -1;
        for (int i = 0; i &lt; n; i++) {
            if (adj[i].size() &lt;= 2) {
                corner = i;
                break;
            }
        }
        
        // If all nodes have degree &gt; 2, it's a single row
        if (corner == -1) {
            vector&lt;vector&lt;int&gt;&gt; result(1);
            vector&lt;bool&gt; visited(n, false);
            result[0].push_back(0);
            visited[0] = true;
            
            for (int i = 1; i &lt; n; i++) {
                for (int neighbor : adj[result[0].back()]) {
                    if (!visited[neighbor]) {
                        result[0].push_back(neighbor);
                        visited[neighbor] = true;
                        break;
                    }
                }
            }
            return result;
        }
        
        // Build first row starting from corner
        vector&lt;int&gt; firstRow;
        vector&lt;bool&gt; visited(n, false);
        firstRow.push_back(corner);
        visited[corner] = true;
        
        // Find next node in first row
        for (int neighbor : adj[corner]) {
            if (adj[neighbor].size() &lt;= 3) {
                int curr = neighbor;
                firstRow.push_back(curr);
                visited[curr] = true;
                
                while (adj[curr].size() == 3 || (adj[curr].size() == 2 &amp;&amp; firstRow.size() &lt; n)) {
                    bool found = false;
                    for (int next : adj[curr]) {
                        if (!visited[next] &amp;&amp; adj[next].size() &lt;= 3) {
                            firstRow.push_back(next);
                            visited[next] = true;
                            curr = next;
                            found = true;
                            break;
                        }
                    }
                    if (!found) break;
                }
                break;
            }
        }
        
        int cols = firstRow.size();
        int rows = n / cols;
        vector&lt;vector&lt;int&gt;&gt; result(rows, vector&lt;int&gt;(cols));
        
        for (int j = 0; j &lt; cols; j++) {
            result[0][j] = firstRow[j];
        }
        
        // Build remaining rows
        for (int i = 1; i &lt; rows; i++) {
            for (int j = 0; j &lt; cols; j++) {
                for (int neighbor : adj[result[i-1][j]]) {
                    if (!visited[neighbor]) {
                        result[i][j] = neighbor;
                        visited[neighbor] = true;
                        break;
                    }
                }
            }
        }
        
        return result;
    }
};</code></pre>
        </div>

        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>
</html>