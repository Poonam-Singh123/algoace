<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>2872. Maximum Number of K-Divisible Components</title>
    <link rel="stylesheet" href="/styles.min.css" />
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>
<body>
    <main>
        <h1>LeetCode 2872: Maximum Number of K-Divisible Components (C++)</h1>

        <div class="problem-description">
            <p><strong>Problem:</strong>
                There is an undirected tree with n nodes labeled from 0 to n - 1. You are given the integer n and a 2D integer array edges of length n - 1, where edges[i] = [a<sub>i</sub>, b<sub>i</sub>] indicates that there is an edge between nodes a<sub>i</sub> and b<sub>i</sub> in the tree. You are also given a 0-indexed integer array values of length n, where values[i] is the value associated with the i<sup>th</sup> node, and an integer k. A valid split of the tree is obtained by removing any set of edges, possibly empty, from the tree such that the resulting components all have values that are divisible by k, where the value of a connected component is the sum of the values of its nodes. Return the maximum number of components in any valid split.
            </p>
            <p><strong>Example:</strong>
                Input: n = 5, edges = [[0,2],[1,2],[1,3],[2,4]], values = [1,8,1,4,4], k = 6<br/>
                Output: 2<br/>
                Explanation: We remove the edge connecting node 1 and 2. The resulting split is valid because: The value of the component containing nodes 1 and 3 is values[1] + values[3] = 12. The value of the component containing nodes 0, 2, and 4 is values[0] + values[2] + values[4] = 6. It can be shown that no other valid split has more than 2 connected components.<br/><br/>
                Input: n = 7, edges = [[0,1],[0,2],[1,3],[1,4],[2,5],[2,6]], values = [3,0,6,1,5,2,1], k = 3<br/>
                Output: 3<br/>
                Explanation: We remove the edge connecting node 0 with 2, and the edge connecting node 0 with 1. The resulting split is valid because: The value of the component containing node 0 is values[0] = 3. The value of the component containing nodes 2, 5, and 6 is values[2] + values[5] + values[6] = 9. The value of the component containing nodes 1, 3, and 4 is values[1] + values[3] + values[4] = 6. It can be shown that no other valid split has more than 3 connected components.
            </p>
        </div>

        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int maxKDivisibleComponents(int n, vector&lt;vector&lt;int&gt;&gt;&amp; edges, vector&lt;int&gt;&amp; values, int k) {
        // Build adjacency list
        vector&lt;vector&lt;int&gt;&gt; adj(n);
        for (auto&amp; edge : edges) {
            adj[edge[0]].push_back(edge[1]);
            adj[edge[1]].push_back(edge[0]);
        }
        
        int components = 0;
        
        function&lt;long long(int, int)&gt; dfs = [&amp;](int node, int parent) -&gt; long long {
            long long sum = values[node];
            
            for (int neighbor : adj[node]) {
                if (neighbor != parent) {
                    sum += dfs(neighbor, node);
                }
            }
            
            // If current subtree sum is divisible by k, we can split it
            if (sum % k == 0) {
                components++;
                return 0;
            }
            
            return sum;
        };
        
        dfs(0, -1);
        
        return components;
    }
};</code></pre>
        </div>

        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>
</html>