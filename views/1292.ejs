<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>1292. Maximum Side Length of a Square with Sum Less than or Equal to Threshold</title>
    <link rel="stylesheet" href="/styles.min.css" />
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>
<body>
    <main>
        <h1>LeetCode 1292: Maximum Side Length of a Square with Sum Less than or Equal to Threshold (C++)</h1>

        <div class="problem-description">
            <p><strong>Problem:</strong>
                Given a m x n matrix mat and an integer threshold, return the maximum side-length of a square with a sum less than or equal to threshold or return 0 if there is no such square.
            </p>
            <p><strong>Example:</strong>
                Input: mat = [[1,1,3,2,4,3,2],[1,1,3,2,4,3,2],[1,1,3,2,4,3,2]], threshold = 4<br/>
                Output: 2<br/>
                Explanation: The maximum side length of square with sum less than 4 is 2 as shown.<br/><br/>
                Input: mat = [[2,2,2,2,2],[2,2,2,2,2],[2,2,2,2,2],[2,2,2,2,2],[2,2,2,2,2]], threshold = 1<br/>
                Output: 0<br/><br/>
                Input: mat = [[1,1,1,1],[1,0,0,0],[1,0,0,0],[1,0,0,0]], threshold = 6<br/>
                Output: 3<br/>
                Explanation: The maximum side length is 3 with sum = 4 which is less than or equal to threshold 6.
            </p>
        </div>

        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int maxSideLength(vector&lt;vector&lt;int&gt;&gt;&amp; mat, int threshold) {
        int m = mat.size();
        int n = mat[0].size();
        
        // Build prefix sum array
        vector&lt;vector&lt;int&gt;&gt; prefix(m + 1, vector&lt;int&gt;(n + 1, 0));
        
        for (int i = 1; i &lt;= m; i++) {
            for (int j = 1; j &lt;= n; j++) {
                prefix[i][j] = mat[i - 1][j - 1] 
                             + prefix[i - 1][j] 
                             + prefix[i][j - 1] 
                             - prefix[i - 1][j - 1];
            }
        }
        
        int maxLen = 0;
        
        // Try all possible squares
        for (int i = 1; i &lt;= m; i++) {
            for (int j = 1; j &lt;= n; j++) {
                // Try all possible side lengths from current position
                int maxPossible = min(m - i + 1, n - j + 1);
                for (int len = 1; len &lt;= maxPossible; len++) {
                    int sum = getSquareSum(prefix, i, j, len);
                    if (sum &lt;= threshold) {
                        maxLen = max(maxLen, len);
                    }
                }
            }
        }
        
        return maxLen;
    }
    
private:
    int getSquareSum(vector&lt;vector&lt;int&gt;&gt;&amp; prefix, int row, int col, int len) {
        int r2 = row + len - 1;
        int c2 = col + len - 1;
        
        return prefix[r2][c2] 
             - prefix[row - 1][c2] 
             - prefix[r2][col - 1] 
             + prefix[row - 1][col - 1];
    }
};</code></pre>
        </div>

        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>
</html>