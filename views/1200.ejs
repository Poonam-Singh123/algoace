<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>1200. Minimum Absolute Difference</title>
    <link rel="stylesheet" href="/styles.min.css" />
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>
<body>
    <main>
        <h1>LeetCode 1200: Minimum Absolute Difference (C++)</h1>

        <div class="problem-description">
            <p><strong>Problem:</strong>
                Given an array of distinct integers arr, find all pairs of elements with the minimum absolute difference of any two elements. Return a list of pairs in ascending order (with respect to pairs), each pair [a, b] follows: a, b are from arr. a &lt; b. b - a equals to the minimum absolute difference of any two elements in arr.
            </p>
            <p><strong>Example:</strong>
                Input: arr = [4,2,1,3]<br/>
                Output: [[1,2],[2,3],[3,4]]<br/>
                Explanation: The minimum absolute difference is 1. List all pairs with difference equal to 1 in ascending order.<br/><br/>
                Input: arr = [1,3,6,10,15]<br/>
                Output: [[1,3]]<br/><br/>
                Input: arr = [3,8,-10,23,19,-4,-14,27]<br/>
                Output: [[-14,-10],[19,23],[23,27]]
            </p>
        </div>

        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    vector&lt;vector&lt;int&gt;&gt; minimumAbsDifference(vector&lt;int&gt;&amp; arr) {
        // Sort the array
        sort(arr.begin(), arr.end());
        
        // Find minimum absolute difference
        int minDiff = INT_MAX;
        for (int i = 1; i &lt; arr.size(); i++) {
            minDiff = min(minDiff, arr[i] - arr[i - 1]);
        }
        
        // Collect all pairs with minimum difference
        vector&lt;vector&lt;int&gt;&gt; result;
        for (int i = 1; i &lt; arr.size(); i++) {
            if (arr[i] - arr[i - 1] == minDiff) {
                result.push_back({arr[i - 1], arr[i]});
            }
        }
        
        return result;
    }
};</code></pre>
        </div>

        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>
</html>