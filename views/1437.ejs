<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 1437: Check If All 1's Are at Least Length K Places Away (C++)
    </title>
    <link rel="stylesheet" href="/styles.min.css" />
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 1437: Check If All 1's Are at Least Length K Places Away (C++)</h1>

        <div class="problem-description">
            <p><strong>Problem:</strong>
                Given a binary array nums of 0s and 1s and an integer k, return true if all 1's are at least k places away from each other, otherwise return false.
            </p>
            <p><strong>Example:</strong>
                Input: nums = [1,0,0,0,1,0,0,1], k = 2<br>
                Output: true<br>
                Explanation: Each of the 1s are at least 2 places away from each other.
            </p>
        </div>

        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    bool kLengthApart(vector&lt;int&gt;&amp; nums, int k) {
        int prevIndex = -1;
        
        for (int i = 0; i &lt; nums.size(); i++) {
            if (nums[i] == 1) {
                // Check distance from previous 1
                if (prevIndex != -1 &amp;&amp; i - prevIndex - 1 &lt; k) {
                    return false;
                }
                // Update previous index
                prevIndex = i;
            }
        }
        
        return true;
    }
};</code></pre>
        </div>

        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>