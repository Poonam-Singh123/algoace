<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>1390. Four Divisors</title>
    <link rel="stylesheet" href="/styles.min.css" />
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>
<body>
    <main>
        <h1>LeetCode 1390: Four Divisors (C++)</h1>

        <div class="problem-description">
            <p><strong>Problem:</strong>
                Given an integer array nums, return the sum of divisors of the integers in that array that have exactly four divisors. If there is no such integer in the array, return 0.
            </p>
            <p><strong>Example:</strong>
                Input: nums = [21,4,7]<br/>
                Output: 32<br/>
                Explanation: 21 has 4 divisors: 1, 3, 7, 21. 4 has 3 divisors: 1, 2, 4. 7 has 2 divisors: 1, 7. The answer is the sum of divisors of 21 only (1 + 3 + 7 + 21 = 32).<br/><br/>
                Input: nums = [21,21]<br/>
                Output: 64<br/><br/>
                Input: nums = [1,2,3,4,5]<br/>
                Output: 0<br/>
                Explanation: None of the integers in the array have exactly 4 divisors.
            </p>
        </div>

        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int sumFourDivisors(vector&lt;int&gt;&amp; nums) {
        int totalSum = 0;
        
        for (int num : nums) {
            int divisorCount = 0;
            int divisorSum = 0;
            
            // Find all divisors up to sqrt(num)
            for (int i = 1; i * i &lt;= num; i++) {
                if (num % i == 0) {
                    divisorCount++;
                    divisorSum += i;
                    
                    // If i is not the square root, count the paired divisor
                    if (i * i != num) {
                        divisorCount++;
                        divisorSum += num / i;
                    }
                    
                    // Early termination if more than 4 divisors
                    if (divisorCount &gt; 4) {
                        break;
                    }
                }
            }
            
            // Add to total sum only if exactly 4 divisors
            if (divisorCount == 4) {
                totalSum += divisorSum;
            }
        }
        
        return totalSum;
    }
};</code></pre>
        </div>

        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>
</html>